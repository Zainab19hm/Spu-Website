Legacy assets that are not part of the shipped site live here.

- `assets-legacy/images/` stores archived images and logo variants that are intentionally excluded from `public/`.
- Files should be moved here only after confirming they are not referenced by source files or generated output.
- `public/` should contain only assets that are actively shipped.
